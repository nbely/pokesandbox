# flowcord

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build flowcord` to build the library.

## Running unit tests

Run `nx test flowcord` to execute the unit tests via [Jest](https://jestjs.io).
