export const RESERVED_BUTTON_LABELS = ['Back', 'Cancel', 'Next', 'Previous'];

export enum MenuResponseType {
  COMPONENT,
  MESSAGE,
  MIXED,
}

export enum MenuPaginationType {
  BUTTONS = 'buttons',
  LIST = 'list',
}
